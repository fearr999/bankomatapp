import { Building2 } from "lucide-react";
import { PlaceholderModule } from "@/components/layout/placeholder-module";

export default function Page() {
  return (
    <PlaceholderModule
      icon={Building2}
      title="CRM"
      description="Карточка клиента: контакты, договоры, объекты, оборудование, заявки и история взаимодействий."
    />
  );
}
